# =============================================================================
# SECURITY WARNING: This is a template file with placeholder values.
# =============================================================================
# 1. Copy this file to .env: cp .env.example .env
# 2. NEVER commit .env files to git (already in .gitignore)
# 3. Generate NEW secrets - DO NOT use these example values in production
# 4. Use different secrets for each environment (dev, staging, prod)
# 5. If a secret is exposed, rotate it immediately
# =============================================================================

# Application
NODE_ENV=development
PORT=3000
CORS_ORIGIN=*

# JWT - Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-secret-key-change-in-production
JWT_EXPIRES_IN=7d

# Database
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=droid_ai_agents
DB_SYNCHRONIZE=true
DB_LOGGING=false
DB_SSL=false

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# AI/LLM Integration
# OpenAI (GPT-4, GPT-3.5-Turbo)
OPENAI_API_KEY=your-openai-api-key

# Anthropic (Claude 3 models)
ANTHROPIC_API_KEY=your-anthropic-api-key

# OpenRouter (Multi-model access including Gemini)
OPENROUTER_API_KEY=your-openrouter-api-key

# Application URL (for OpenRouter referrer)
APP_URL=http://localhost:3000
